export function findMax(arrayNumbers) { 
 let Max = Math.max (...arrayNumbers);   
 return Max;
 }

 export function reverseArray(arrayNumbers) {  
  return arrayNumbers.reverse();
  console.log('Reversed: ', reverseArray([1, 2, 3])); 
 }


 